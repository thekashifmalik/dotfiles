#!/bin/bash
set -eo pipefail

[[ -z $1 ]] && echo "No key provided" && exit 1
KEY=$1
[[ -z $2 ]] && echo "No file provided" && exit 1
FILE=$2

IV=`head -1 $FILE`
tail -n +2 $FILE | openssl enc -d -v -aes-256-cbc -K $KEY -iv $IV
