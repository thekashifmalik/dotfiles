#!/usr/bin/env bash
set -eo pipefail

if [[ "$OSTYPE" =~ ^darwin ]]; then
    brew update
    brew install git
fi

if [[ "$OSTYPE" =~ ^linux ]]; then
    sudo apt update
    sudo apt install -y git
fi

ln -fsv $PWD/git/.gitconfig ~
