#!/bin/bash
set -eo pipefail

echo "linking .profile"
ln -sf ${PWD}/etc/.profile ~/.profile

echo "linking .bash_aliases"
ln -sf ${PWD}/etc/.bash_aliases ~/.bash_aliases

echo "source .profile to update environment"

