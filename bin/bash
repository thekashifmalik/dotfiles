#!/bin/bash
set -eo pipefail

echo
echo "linking bash config"
ln -fsv $PWD/etc/.bashrc ~/.bashrc
ln -fsv $PWD/etc/.bash_profile ~/.bash_profile
ln -sfv $PWD/etc/.bash_aliases ~/.bash_aliases

echo
echo "source .bashrc to update environment"
