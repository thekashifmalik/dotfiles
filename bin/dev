#!/bin/bash
set -eo pipefail

# Dependencies.
bin/bash

echo "installing packages"

# MacOS
if [[ "$OSTYPE" =~ ^darwin ]]; then
    echo "detected MacOS"
    brew update
    brew upgrade
    brew install tmux
fi

# Linux
if [[ "$OSTYPE" =~ ^linux ]]; then
    echo "detected Linux"
    sudo apt-get update
    sudo apt-get install tmux
fi


echo "linking .tmux.conf"
ln -sf ${PWD}/etc/.tmux.conf ~/.tmux.conf

echo "linking .gitconfig"
ln -sf ${PWD}/etc/.gitconfig ~/.gitconfig

# # set up vim
# mkdir -p ~/.config/nvim/init.vim && ln -sf ${PWD}/etc/init.vim ~/.config/nvim/init.vim
# curl -fLo ~/.config/nvim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

# # TODO: We need to copy the monokai.vim file somewhere for vim syntax highlighting to ∑ork.
# # cp vendor/molokai/colors
