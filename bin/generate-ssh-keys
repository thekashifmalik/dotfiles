#!/bin/bash
set -eo pipefail

read -p "Enter name for SSH key: " KEY_NAME

# Create SSH key
ssh-keygen -t ed25519 -C "$KEY_NAME" -f "$KEY_NAME.key" -N ""

# Add key to SSH agent.
#eval "$(ssh-agent -s)"
#ssh-add "$OUTPUT_FILENAME"
