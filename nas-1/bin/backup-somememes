#!/usr/bin/env bash
set -eo pipefail

echo "Backing up: somememes"

ssh desktop-1 "cd ~/somememes && bin/pg_dump > db.sql"

rsync -hav --delete \
    desktop-1:~/somememes/db.sql \
    desktop-1:/mnt/HDD-3/somememes/media \
    /volume1/somememes

ssh desktop-1 "cd ~/somememes && rm -v db.sql"
