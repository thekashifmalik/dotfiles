#!/usr/bin/env bash
set -eo pipefail

HOSTNAME=laptop-3
echo "Backing up: $HOSTNAME"

rsync -hav --delete \
    "$HOSTNAME:~/Logic" \
    "$HOSTNAME:~/No\ Crimson" \
    "$HOSTNAME:~/Photography" \
    /volume1/$HOSTNAME

rsync -hav --delete "$HOSTNAME:~/Pictures/Lightroom/Backups/" /volume1/$HOSTNAME/Lightroom
